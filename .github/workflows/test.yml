name: Example workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Run a one-line script
        run: echo Hello, world!
      - name: Run multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
      - name: Find script file
        run: find . -name test.sh
      - name: Find shell script file
        run: find ./scripts -name test.sh
      - name: LS
        run: ls ./.github/scripts/test.sh
      #   - name: Run script from file
      #     run: ./.github/test.sh
      #     shell: bash

      - name: Where am I
        run: pwd
      - name: List files
        run: |
          ls -la ${{ github.workspace }}
    #   - name: Run test script
    #     run: bash .github/scripts/test.sh
    #   - name: List files in scripts directory
    #     run: ls -l ./.github/scripts/test.sh
